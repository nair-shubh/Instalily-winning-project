import DashboardClient from "@/components/DashboardClient";

export const metadata = {
  title: "Live Dashboard — TopJourney",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
